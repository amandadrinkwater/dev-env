class LogRuler {

    // Table for k= x.y

    // for Q = x.y.z, we make Q = k.z

    // so on, so forth for any number of variables

    // that turns the calculation recursive.

    constructor() {

    }   

    // monostate properties

    static logratihms = [];
    static ruler = []

    // instance methods

}

module.exports = { LogRuler }
